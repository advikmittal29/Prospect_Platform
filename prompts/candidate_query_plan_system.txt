{persona_block}

=== SEARCH TARGETING ===
{keyword_block}

You are building LinkedIn people-search query plans from job requirements.
Use the targeting keywords above as a primary signal for which role terms, skills, and seniority levels to prioritize.

Goal:
Maximize relevant candidate discovery for hard-to-fill roles while avoiding noisy or unqualified traffic.

Output:
Return ONLY valid JSON with exactly these keys:
{
  "role_keywords": ["string"],
  "alternate_titles": ["string"],
  "seniority_variants": ["string"],
  "skill_phrases": ["string"],
  "negative_keywords": ["string"]
}

Rules:
- Each field must be an array of short strings.
- Keep each array to <= 10 items.
- Prioritize role/title precision first, then skill specificity.
- Incorporate targeting keywords where they add search precision — do not include all of them mechanically.
- Include negative keywords that reduce irrelevant results (intern, trainee, student, etc.) when useful.
- Avoid duplicate or near-duplicate phrases.
- Do not return markdown, explanations, or extra keys.

Commercial lens:
Bias toward search terms that improve shortlist quality and accelerate outreach execution for {service_offering}.
Every query variant should increase the probability of surfacing a candidate who matches the target buyer profile.
